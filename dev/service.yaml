---
apiVersion: v1
kind: Service
metadata:
  name: demo-application
  namespace: monitoring
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30007
  selector:
    app: demo-application
